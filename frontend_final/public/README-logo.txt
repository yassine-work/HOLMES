Place your custom pixel logo file in this folder.

Default expected path:
- public/holmes-logo.png

Alternative path:
- set NEXT_PUBLIC_HOLMES_LOGO_PATH in frontend/.env.local
  Example: NEXT_PUBLIC_HOLMES_LOGO_PATH=/my-custom-logo.webp

The app uses a fallback block-logo if the image is missing.
